{
  "name": "postcode-search",
  "version": "0.0.0",
  "description": "Check postcode with AusPost API",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watchAll",
    "lint": "prettier --check {src,tests}/**/*.js && eslint {src,tests}/**/*.js",
    "lint:fix": "prettier --write {src,tests}/**/*.js"
  },
  "private": true,
  "author": "Ricardo Furtado",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^6.5.1",
    "eslint-plugin-jest": "^22.17.0",
    "jest": "^24.9.0",
    "prettier": "^1.18.2"
  }
}
